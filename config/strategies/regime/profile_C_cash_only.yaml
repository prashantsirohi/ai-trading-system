name: profile_C_cash_only
# risk_per_trade_pct is the max account % at risk per individual trade
# (sizing input for the stop-distance position sizer). Phase 6:
# centralizes here so risk scales with regime rather than living in the
# signal payload. risk_off=0 because max_positions=0 anyway.
risk_off:      { min_score: 82, rank_top_n: 10, max_exposure: 0.0,  max_positions: 0,  max_sector_exposure: 0.20, max_single_stock_weight: 0.07, atr_stop_mult: 2.0, breakout_mode: strict,     allow_pyramiding: false, risk_per_trade_pct: 0.0 }
neutral:       { min_score: 72, rank_top_n: 20, max_exposure: 0.50, max_positions: 6,  max_sector_exposure: 0.25, max_single_stock_weight: 0.08, atr_stop_mult: 2.3, breakout_mode: normal,     allow_pyramiding: false, risk_per_trade_pct: 0.40 }
cautious_bull: { min_score: 68, rank_top_n: 25, max_exposure: 0.60, max_positions: 8,  max_sector_exposure: 0.28, max_single_stock_weight: 0.09, atr_stop_mult: 2.4, breakout_mode: strict,     allow_pyramiding: false, risk_per_trade_pct: 0.55 }
bull:          { min_score: 64, rank_top_n: 40, max_exposure: 0.85, max_positions: 10, max_sector_exposure: 0.32, max_single_stock_weight: 0.10, atr_stop_mult: 2.6, breakout_mode: normal,     allow_pyramiding: true,  risk_per_trade_pct: 0.75 }
strong_bull:   { min_score: 58, rank_top_n: 60, max_exposure: 1.00, max_positions: 14, max_sector_exposure: 0.38, max_single_stock_weight: 0.12, atr_stop_mult: 3.0, breakout_mode: aggressive, allow_pyramiding: true,  risk_per_trade_pct: 1.00 }
